# Information-limit integration map on 2026-09-15

## Main article

The body retains only the consequences required to interpret the experiments: the simple equal-covariance Gaussian map from quotient distance to oracle error, the order-dependent local resolution scale, the impossibility of uniform exact-support recovery without a minimum severity, and a Fano necessary condition for a finite support bank. The discussion uses the archived RAWSIM39 checkpoint to show that screening, conditioning, residual rejection, and nonlinear warnings are different gates.

## Supplementary material

Appendix D carries the complete prefix-monotonicity proof through the Schur complement, its equality condition, temporal-rescue examples, contact-order and information-rate consequences, the binary and minimax decision bounds, and the Fano support limit. Appendix E carries the exact-redundancy reduction and the mixed regular/order-$k$ evidence asymptotic. The RAWSIM39 table records the frozen gate taxonomy and explicitly stops before assigning a contact order.

## Reproducible evidence

`scripts/verify_information_limits.py` generates `generated/information_limit_checks.json`, `tables/generated/information_limit_validation.tex`, and `tables/generated/rawsim39_v12_taxonomy.tex`. It runs eleven controlled mathematical checks plus one audit of the frozen RAWSIM39 ledger. The source branch, commit, paths, and SHA-256 hashes of the five imported checkpoint files are recorded in the frozen source manifest and snapshot.

## Deferred claims

The current checkpoint does not establish that a curvature warning is second order, that a near-redundant direction is exactly redundant, or that the reported condition numbers predict localization error. Those claims require cumulative prefix likelihoods, higher directional derivatives, nonlinear TDS convergence, an independent operating point, and a pairwise distance-versus-error campaign. The low-rank discrepancy-retention identity is now established for the stated whitened covariance model, but its event dictionary, mismatch covariance, learned correction, and shifted IEEE 39-bus benefit remain research gates rather than empirical manuscript evidence.
