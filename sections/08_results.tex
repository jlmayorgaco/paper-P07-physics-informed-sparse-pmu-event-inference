\section{Uncertainty, Physical Mismatch, and Adaptation}
\label{sec:results}

\subsection{Marginal Calibration Does Not Whiten Innovations}

The nominal B2 covariance is conservative. Two temperatures fitted on 30 independent CAL trajectories bring TEST marginal coverage closer to 95\% and improve hidden NLL, but do not change lag-one innovation autocorrelation or the rejection of whiteness. Four principal components explain 99.2\% of calibration-residual variance, yet a rank-four measurement Gauss--Markov correction worsens HVRE and undercovers both voltage components; process placement is neutral. Marginal calibration, temporal adequacy, and useful discrepancy structure are therefore distinct properties. The complete diagnostics remain generated from the frozen artifacts but are omitted here to preserve space for the prospective event results.

\subsection{Mismatch Response Depends on Model Family}

The rebuilt-plant campaign reveals a clear family dependence (Fig.~\ref{fig:physical-mismatch}). At the largest tested scale, network mismatch raises the median error by \NetworkMismatchRatio{}$\times$ and an operating-point shift raises it by \OperatingMismatchRatio{}$\times$. Machine, governor, AVR, and load-model perturbations remain near the nominal median because this 3-s excitation does not measurably expose those parameter changes. Their near-$1\times$ ratios are regime-limited null responses, not evidence of robustness. The tested estimator fails visibly when the algebraic network map or equilibrium center changes.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/generated/physical_mismatch.pdf}
\caption{Physical-mismatch atlas for the fixed nominal estimator. (a) Median B2 hidden-voltage HVRE relative to the pooled $m=0$ median for every family--scale cell; annotations show the untransformed ratio and color encodes its base-10 logarithm. (b) Mean nominal-U2 marginal 95\% coverage under the same rebuilt plants. (c) Macro adequacy discrimination from observed innovations; undefined family-level AUROCs are omitted from the macro mean. Each family--scale cell contains 20 independently rebuilt plants.}
\label{fig:physical-mismatch}
\end{figure*}

Nominal covariance repair does not transfer automatically. Averaged across all mismatch cases, U0 has \MismatchUzeroCoverage{} 95\% coverage and nominally fitted U2 has only \MismatchUtwoCoverage{}. The reduction reflects physical shift, not a calibration/test leak: U2 remains frozen from nominal CAL data. This result limits the interpretation of Section~\ref{sec:estimation}: nominal dispersion scaling is not robustness to a changed plant.

\subsection{Adequacy Detection and Physical Recentering}

The observed-PMU innovation statistic detects many cases in which hidden HVRE is unacceptable. At the largest tested scale, its macro AUROC/AUPRC is \AdequacyMaxAUROC{}/\AdequacyMaxAUPRC{} [Fig.~\ref{fig:physical-mismatch}(c)]. Family-level AUROC is omitted when every frame has the same frozen adequacy label; AUPRC remains defined. The statistic can flag a stale model, but it neither identifies the mismatch family nor supplies a calibrated alarm probability.

The earlier R2-A offset-only ablation remains scientifically useful because it isolates what does not solve the problem. Across nonnominal identifiable M1, M6, and M7 cases it closes only \OnlineClosurePercent{} of the oracle gap, despite passing its nominal-safety gate. That failure does not imply that adaptation from observed PMUs is impossible; it shows that grouped voltage offsets without AC balance and a rebuilt physical center are inadequate.

The regularized-recentering experiment tests that distinction for M6. Every TEST case satisfies the prespecified convergence gate. Across the three mismatch scales, the physical solve removes \EZeroHClosureHalf{}--\EZeroHClosureMax{} of the error gap to an oracle center [Fig.~\ref{fig:adequacy-adaptation}(a)--(b)]. The median solve takes \EZeroHRuntimeMedian{}~ms. Table~\ref{tab:e06h-results} retains the point errors, paired intervals, and tail runtime; these static timings do not establish end-to-end real-time operation.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/generated/adequacy_adaptation.pdf}
\caption{Corrected regularized physical recentering for held-out M6 operating-point mismatch. (a) Median hidden-voltage HVRE for the nominal center and the physical solve. (b) Paired oracle-gap closure with 95\% bootstrap intervals over 20 independent TEST cases per scale; the oracle-center reference is 100\%. (c) Corrected point error versus the median joint NEES-like diagnostic. Marginal 95\% coverage is 100\% at each scale, but the joint statistic remains far above its nominal reference.}
\label{fig:adequacy-adaptation}
\end{figure*}

\input{tables/generated/e06h_results}

The nuisance information matrix has median rank \EZeroHRank{} among \EZeroHDimension{} coordinates. Even with that nullspace, the requested voltage field is recovered with two orders of magnitude less error than the frozen center. This supports target recovery despite nuisance non-uniqueness. It does not prove exact functional identifiability because the local hidden-voltage residual remains nonzero, nor does it show that the injection vector is recovered.

Point accuracy does not certify posterior uncertainty. The pseudoinverse Laplace covariance is formed only on locally informed nuisance directions; because $\bm J_{zd}\bm N(\bm J_{yd})\ne\bm0$ in \eqref{eq:e06h-functional-residual}, it does not represent the image of the PMU-uninformed nuisance nullspace in the hidden-voltage target. This explains how marginal intervals can cover every component while the joint NEES-like statistic grows with mismatch and remains far above its nominal reference [Fig.~\ref{fig:adequacy-adaptation}(c)]. The independent constrained solver also converges on only part of its small diagnostic subset. M6 point reconstruction is supported; joint uncertainty, network mismatch, coupled mismatch, and sequential adaptation are not.

\subsection{Prospective Load Cardinality and Support Recovery}

The single-source development path establishes the components used by the larger support test. The centered numerical tangent differs from either one-sided derivative by at most \LoadTangentMaxRelError{}, W2 yields NIS/frame \LoadWTwoNIS{} with lag-one correlation \LoadWTwoACFOne{}, and the fitted nonlinear remainder has order $\LoadTruncationSlope{}$ with bootstrap interval $[\LoadTruncationCILow{},\LoadTruncationCIHigh{}]$. The 137-hypothesis confirmation then freezes $D$, $Q$, $Q_{ij}$, W2, priors, onset, and GH31 before generating new trajectories.

All \GlobalPhysicalTrajectories{} physical trajectories succeed, and none of their identifiers overlaps the 1,816 historical identifiers checked. Table~\ref{tab:global137-results} reports \GlobalNoiseRecords{} complete-record decisions. Weak--weak events are the limiting regime: cardinality accuracy is \GlobalWeakCardinality{}, exact-support Top-1 is \GlobalWeakTopOne{}, and Top-3 is \GlobalWeakTopThree{}. Weak--strong Top-1 rises to \GlobalWeakStrongTopOne{}, while moderate and finite Top-1 reach \GlobalModerateTopOne{} and \GlobalFiniteTopOne{}. These are support decisions over all 120 pairs, not two independent single-source classifications.

\input{tables/generated/global137_results}

The weak transition isolates what improves first. As the larger weak magnitude rises from 0.033\% to 0.121\%, mean $p(K=2)$ increases from 0.088 to 0.758, whereas exact-support Top-1 remains \GlobalWeakTopOne{} [Fig.~\ref{fig:multi-event-evidence}(b)]. The model can therefore accumulate evidence for two active sources while remaining unsure which pair produced the response. Over all event records, detection AUROC/AUPRC is \GlobalEventAUROC{}/\GlobalEventAUPRC{}, with \GlobalEventFNR{} false negatives at the 0.5 threshold. The observed $H_0$ false-positive count is zero, but only 100 $H_0$ records were exposed; its 95\% upper bound is \GlobalEventFPRUpper{}, so this is not a continuous-alarm guarantee. Per-bus inclusion has AUROC \GlobalInclusionAUROC{} and ECE \GlobalInclusionECE{}, but recall 0.645 shows that calibrated-looking marginal scores do not repair weak exact-support recovery.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/generated/multi_event_evidence.pdf}
\caption{From prospective recovery to its information boundary. (a) Frozen GLOBAL-137 cardinality and exact-support results. (b) Within weak--weak cases, cardinality evidence grows while exact Top-1 remains flat. (c) Retrospective-only mechanism audit: the global profiled manifold distance predicts $p(K=2)$ and exact recovery more strongly than a support-only score. (d) Nominal-operating-point T120 validation: pairwise interaction $Q_{ij}$ is necessary to track simultaneous nonlinear responses. (e) The within-artifact separation ratio grows through 120 frames, alongside measured GH31 runtime; absolute separation values are withheld because their scale is not reconciled with the earlier profiled metric. (f) The five smallest equilibrium singular values remain positive but accompany near-unit signature coherence. Panels (a)--(b) are prospective primary evidence; (c) is retrospective; (d)--(f) are contract diagnostics, not recovery claims.}
\label{fig:multi-event-evidence}
\end{figure*}

The retrospective audit supplies a mechanism without changing the frozen estimator. Across 2,400 weak-regime cases, global profiled distance correlates with exact support at $\rho=\WeakGlobalExactRho{}$, compared with $\rho=\WeakSupportOnlyExactRho{}$ for the support-only quantity [Fig.~\ref{fig:multi-event-evidence}(c)]. All tested local directions are first-order resolvable; the curvature-dominated explanation and simple $a^{-2}$ or $a^{-4}$ resolution-delay laws are not supported. This negative result replaces the earlier mismatched comparison between pairwise Fisher information and pooled 16-way confusion.

The T120 diagnostic narrows the structural interpretation. The within-artifact global separation proxy grows monotonically and is \TOneTwentyInfoRatio{} times its T30 value at T120; $\gamma_{4,\infty}=\TOneTwentyGammaFour{}>0$, and no equilibrium pair is numerically degenerate in the frozen local model. This does not establish universal source identifiability. The hardest pair, buses 26--28, has positive $\sigma_{\min}=\TOneTwentyHardPairSigma{}$ but coherence \TOneTwentyHardPairCoherence{}; buses 7--12 have coherence \TOneTwentySevenTwelveCoherence{}. Distinct signatures can therefore be severely ill-conditioned.

Physical validation also reveals why the mixed term matters. At T120, the median whitened response error is \TOneTwentyDMedianError{} for $D$, \TOneTwentyDQMedianError{} for $D+Q$, and \TOneTwentyCompleteMedianError{} for $D+Q+Q_{ij}$. For the complete quadratic model, median error relative to the nearest margin is \TOneTwentyCompleteMedianEta{} and the maximum is \TOneTwentyCompleteMaxEta{}. The 32-path validation bank varies pair, sign, and amplitude but not operating point, so its status remains limited. Full 137-hypothesis GH31 takes \TOneTwentyRuntime{}~s at T120 on the benchmark; this is computational evidence, not a real-time claim. Independent operating-point validation and scale reconciliation are required before the T120 contract is frozen for prospective recovery.

\subsection{Known-Source Detection and Localization}

On the known-source TEST split, the availability-gated hierarchy reaches detection F1 \LegacyEventDetectionFOne{}, event macro-F1 \LegacyEventMacroFOne{}, physical Top-1/Top-3 \LegacyPhysicalTopOne{}/\LegacyPhysicalTopThree{}, and integrity Top-1 \LegacyIntegrityTopOne{}. Expanding the representation adds \LegacyPhysicalGainPP{} percentage points to physical-source ranking over the compact hierarchy. Multiscale dynamics, adaptive residuals, and PMU contrasts help when the source is represented during training; the availability rule contributes mainly to integrity handling.

The gap between detection F1 and physical Top-1 confirms that a recognizable disturbance need not have a uniquely recoverable source. False alarms decrease to \LegacyFalseAlarmsPerMinute{} episodes per normal-labeled minute, but the normal exposure is only about 7.15~min per split. That rate is evidence of alarm burden in a short synthetic stream, not an operational false-alarm guarantee.

\subsection{Source Holdout Breaks Learned Localization}

With the physical source excluded from source-labeled fitting and the true event family supplied, only \HeldSourceCorrect{} of \HeldSourceDecisions{} decisions are exact. Detection and event-family confusion have been removed from this test. The remaining error shows that the learned location heads and candidate descriptors retain source-specific associations, invalidating a zero-shot-source claim for the legacy system.

Transfer to RAW0001 is also weak: the basic candidate model localizes only \RawCandidatePhysicalEpisodes{} of five physical episodes on average. The simulator bank omits some channels used by the longer-window implementation, so no single feature family can be blamed for the deficit. Internal simulation accuracy does not establish cross-record reliability.

Supervised, physics-guided features work for detection and known-source diagnosis but not for source-generalizable inference. The physical load experiment supplies a contrasting result: all single and double supports are evaluated through simulated responses rather than learned asset labels. Its success is restricted to one event family, fixed topology, known onset, and a matched simulator; it does not repair the broader held-source or cross-record failure.
