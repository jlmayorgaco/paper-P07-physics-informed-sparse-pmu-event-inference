# Tables

Add modular LaTeX tables here only when their numerical evidence has been frozen and reviewed. The table roadmap is in [`../docs/experiment-plan.md`](../docs/experiment-plan.md).

Every released table must identify its campaign, manifest, source-exclusion rule, PMU membership condition, simulator, repetitions or seeds, and denominator. Do not create publication-looking rows of dashes or expected values.
